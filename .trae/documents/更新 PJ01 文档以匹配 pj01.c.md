# PJ01 部分更新计划

## 目标

* 将 PJ01 的属性从 `pwm_duty` 改为 `power(0-255)`

* 移除 `motor_control` 动作描述

* 更新示例为设置/读取 `power`

## 替换内容（用于覆盖文档中 PJ01 章节）

## PJ01 PWM电机控制器

**设备类型**: `PJ01`\
**描述**: PWM电机控制器，使用 `power`(0-255) 控制输出功率，无电池设计

### 设备属性

| 属性名     | 类型  | 读写权限 | 描述   | 默认值 | 范围    |
| ------- | --- | ---- | ---- | --- | ----- |
| `power` | int | 读写   | 功率控制 | 0   | 0-255 |

### 功能说明

* **PWM控制**: 占空比 `duty = 1023 - power * 4`；`power=0→duty=1023`，`power=255→duty=0`

* **PWM参数**: 10位分辨率(0-1023)、频率1000Hz

* **实时响应**: 属性变化立即生效

### 设备动作

* 暂无

### 使用示例

```json
// 设置功率为50%
{
  "method": "set",
  "key": "power",
  "value": 128,
  "msg_id": 1001
}

// 设置功率为最大值
{
  "method": "set",
  "key": "power",
  "value": 255,
  "msg_id": 1002
}

// 停止电机
{
  "method": "set",
  "key": "power",
  "value": 0,
  "msg_id": 1003
}
```

#### 读取当前功率

```json
{
  "method": "get",
  "key": "power",
  "msg_id": 1004
}
```

### 注意事项

1. **功率范围**: `power` 必须在 0-255
2. **无电池设计**: 不包含 `battery` 属性

